<%= form_for([:dashboard, @job]) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row-fluid">
    <div class="span4"><%= f.label :name %><br></div>
    <div class="span8"><%= f.text_field :name %></div>
  </div>
  <div class="row-fluid">
    <div class="span4"><%= f.label :description %><br></div>
    <div class="span8"><%= f.text_area :description, :input_html => {:maxlength => 15, :size => 40} %></div>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
