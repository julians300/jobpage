<h1><%= @biz.companyname %> Listings</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
  </thead>

  <tbody>
    <% @bizjobs.each do |job| %>
      <tr>
        <td><%= link_to job.name, "job_path" %></td>
      </tr>
    <% end %>
  </tbody>
</table>